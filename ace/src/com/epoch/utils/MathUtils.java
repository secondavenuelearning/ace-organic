package com.epoch.utils;

/** Holds math utility functions. Same as MathMethods, but with more conventional
 * name.  We use this stratagem to avoid conflict with Apache's MathUtils.  */
public final class MathUtils extends MathMethods {

	/** Disables external instantiation. */
	private MathUtils() { }
	
} // MathUtils
